#if 0

/*
 * 利用信号量实现进程互斥
 */

semaphore S = 1;               // 初始化信号量，初值为 1

P1() {
    ...
    P(S);                      // 申请临界资源，加锁
    进程 P1 的临界区;
    V(S);                      // 释放临界资源，解锁
    ...
}

P2() {
    ...
    P(S);                      // 申请临界资源，加锁
    进程 P2 的临界区;
    V(S);                      // 释放临界资源，解锁
    ...
}

#endif